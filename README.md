# Normalizeurl

A Ruby library for normalising URLs by removing tracking parameters, session IDs, and other extraneous elements whilst preserving important parameters.

## Installation

Add this line to your application's Gemfile:

